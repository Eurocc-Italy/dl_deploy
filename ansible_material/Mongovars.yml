---  #insert local path to the image dir
db_name: my_database               #insert the name of the database 
collection_name: my_collection     #insert the name you want to give to your collection 
import_file: /home/centos/ansible_material/data.csv
file_type: csv                        #csv, json
export_file: /home/centos/ansible_material/query.csv
export_fields: image_id,age,sex       # insertthe fields to export in the output file
query_field: age                      # the field to query on
query_value: 30                       # the value to query on
query_operator: "$lt"                 # the operator to use in the query


